export default function CommandPanel() {
  return (
    <div
      style={{
        padding: 12,
        fontFamily: "monospace",
        fontSize: 10,
        color: "rgba(0,200,255,0.8)",
      }}
    >
      COMMAND PANEL
    </div>
  );
}
